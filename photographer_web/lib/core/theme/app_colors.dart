import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand
  static const primary = Color(0xff6C3EF4);
  static const secondary = Color(0xff8B5CF6);
  static const accent = Color(0xffA855F7);

  // Background
  static const background = Color(0xffF7F8FD);
  static const sidebar = Color(0xffffffff);
  static const card = Colors.white;

  // Text
  static const heading = Color(0xff1C1B2E);
  static const body = Color(0xff6B7280);

  // Border
  static const border = Color(0xffECECF3);

  // Success
  static const success = Color(0xff22C55E);

  // Warning
  static const warning = Color(0xffF59E0B);

  // Error
  static const danger = Color(0xffEF4444);
}